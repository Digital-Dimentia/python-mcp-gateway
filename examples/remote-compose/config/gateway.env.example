# Copy to gateway.env beside this file. Never commit the copy.
#
# No WS_ACCESS_KEY: in remote mode the gateway binds loopback and SSH is the authentication.
# Setting one would make the desktop app's keyless connection a 401.

SEARCH_TOKEN=
